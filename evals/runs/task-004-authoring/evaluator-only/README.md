# TASK-004 Evaluator-Only Material

This directory is never copied into a model workspace.

It contains the hidden regression test, the independent verifier, reference
and deliberately incomplete repairs, and authoring notes needed to validate
the task before it is frozen. Its contents must not be used to tune a
ReasonKit candidate or to provide hints during an evaluation arm.
